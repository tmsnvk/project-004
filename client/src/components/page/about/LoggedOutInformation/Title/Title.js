import React from "react";
import { MessageTitle } from "components/shared/general";

const Title = () => {
  return (
    <MessageTitle>
      Welcome, adventurer!
    </MessageTitle>
  );
};

export default Title;